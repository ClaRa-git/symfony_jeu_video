{% extends 'base.html.twig' %}

{% block body %}
	{# On récupère le template du composant _detail_card.html.twig #}
	{% include 'components/_detail_card.html.twig' with {'game': game, 'consoles': consoles} %}

	<div class="action-buttons">
		<div>
			{% include "game/_delete_form.html.twig" with {'button_class': 'btn btn-delete'} %}
		</div>

		<div>
			<a href="{{path('app_game_edit', {'id': game.id})}}" class="btn btn-edit">Editer</a>
		</div>

		<div>
			<a href="{{path('app_admin')}}" class="btn btn-back">
				<i class="fas fa-arrow-left"></i>
				Retour à la liste
			</a>
		</div>
	</div>
{% endblock %}
