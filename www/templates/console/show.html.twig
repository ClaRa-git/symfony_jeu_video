{% extends 'base.html.twig' %}

{% block title %}Console{% endblock %}

{% block body %}

    <div class="game-list-container">

        <h1>Console</h1>

        <table class="game-table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Label</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>{{ console.id }}</td>
                    <td>{{ console.label }}</td>
                </tr>
            </tbody>
        </table>
        <div class="action-buttons">
            <div>
                {{ include('console/_delete_form.html.twig') }}
            </div>
            <div>
                <a href="{{ path('app_console_edit', {'id': console.id}) }}" class="btn btn-edit">Editer</a>
            </div>
            <div>
                <a href="{{ path('app_console_index') }}" class="btn btn-back">
                    <i class="fas fa-arrow-left"></i>
                    Retour à la liste
                </a>
            </div>
        </div>
    </div>

{% endblock %}
