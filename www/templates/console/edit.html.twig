{% extends 'base.html.twig' %}

{% block title %}Edition de console{% endblock %}

{% block body %}

    <div class="game-form-container">

        <h1>Modifier la console</h1>

        {{ include('console/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="action-buttons">
            <a href="{{ path('app_console_index') }}" class="btn btn-back">
                <i class="fas fa-arrow-left"></i>
                Retour à la liste    
            </a>
            {{ include('console/_delete_form.html.twig') }}
        </div>

    </div>
{% endblock %}
